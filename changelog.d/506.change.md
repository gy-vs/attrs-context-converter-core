It is now possible to wrap a converter into `attrs.Converter` and get the current instance and/or the current field definition (`attrs.Attribute`) passed into the converter callable using the *takes_self* and *takes_field* arguments.
